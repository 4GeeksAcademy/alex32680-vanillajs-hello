let multiplictionTable =[];

for (let i=1; i<11; i++){
    for(let j=1;j<11; j++) 
    multiplictionTable.push('${i} * ${i*j}')
}

    let randomNumber = Math.random() *10;
    console.(multiplictionTable)

   let pronoun = ['the','our','very','cool', 'super'];
   let adj = ['great','big','tech', 'stack', 'geeks', 'computing'];
   let noun = ['jogger', 'racoon'];

   for (let i=1; i<11; i++){
    for(let j=1;j<11; j++) 
    multiplictionTable.push('${i} * ${j} = ${*j}')
   }
   